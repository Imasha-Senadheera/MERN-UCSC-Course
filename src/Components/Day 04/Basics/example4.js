function multiply(num1, num2) {
  // This function returns the multiplication of num1 and num2
  return num1 * num2;
}

// Declare variables
let x = 2;
let y = 4;

// Display the answer returned by the multiply function
console.log("Multiplication of", x, "and", y, "is", multiply(x, y));
